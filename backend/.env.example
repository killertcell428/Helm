# コピーして .env を作成し、値を設定してください。
# cp .env.example .env   # または手動で .env を作成

# API Key 認証（オーナー用キー1本の例）。有効にすると /api/* は X-API-Key 必須になります。
# 本番では必ず別のキーに変更し、環境変数や Secret Manager で管理してください。
API_KEYS=[{"key":"helm-owner-dev-key","role":"owner"}]
